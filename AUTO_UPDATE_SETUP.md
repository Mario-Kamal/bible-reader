# خطوات إعداد التحديث التلقائي

## ✅ تم إنجازه:

1. **`src/utils/updateManager.ts`** - برنامج التحقق من التحديثات
2. **`src/hooks/useAutoUpdate.ts`** - React hook مدمج في التطبيق
3. **`.github/workflows/update-version.yml`** - GitHub Actions لـ CI/CD تلقائي
4. **`scripts/update-version.js`** - script لتحديث يدوي
5. **`public/version.json`** - ملف الإصدار الحالي

---

## 🚀 الآن ستحتاج:

### 1️⃣ **ربط GitHub مع Lovable** (إذا لم تكن موصول):

```
في Lovable:
- اذهب إلى Settings
- قم بـ Connect GitHub Repo
- اختر التطبيق الحالي
```

### 2️⃣ **التأكد من وجود GitHub Repository**:

```
يجب أن يكون الـ repo موجود على GitHub
وأن تكون الـ branch الرئيسية `main`
```

### 3️⃣ **عند publish من Lovable**:

```
1. اعمل التغييرات في Lovable
2. اضغط Publish أو Commit
3. يتم الدفع إلى GitHub (main branch)
4. GitHub Actions يعمل تلقائياً ✓
5. الـ version.json يتحدث في النهاية
```

### 4️⃣ **المستخدمون في الموبايل**:

```
- عند فتح التطبيق: يتحقق من version.json
- إذا وُجد إصدار أحدث: يعرض إشعار
- ينتظر 2 ثانية ثم يعد تحميل التطبيق تلقائياً
- يحصلون على أحدث نسخة!
```

---

## 📝 اختياري - تخصيص الإعدادات:

### تغيير فترة التحقق (كل ساعة حالياً):

في `src/utils/updateManager.ts`:

```typescript
const VERSION_CHECK_INTERVAL = 60 * 60 * 1000; // غيّر الرقم
// 60000 = دقيقة واحدة
// 300000 = 5 دقائق
// 1800000 = 30 دقيقة
```

### تغيير مدة إعادة التحميل:

في `src/utils/updateManager.ts`:

```typescript
setTimeout(() => {
  window.location.reload();
}, 2000); // غيّر 2000 إلى مدة أقصر أو أطول
```

### تغيير رسالة الإشعار:

في `src/hooks/useAutoUpdate.ts`:

```typescript
toast({
  title: "تحديث جديد متاح",
  description: "غيّر هذا النص حسب رغبتك",
  duration: 3000,
});
```

---

## 🧪 اختبار محلي (اختياري):

```bash
# تحديث الإصدار يدوياً
npm run update-version

# بناء التطبيق
npm run build

# المعاينة
npm run preview
```

---

## ⚡ الميزات:

✅ تحديث تلقائي تماماً - سلس وسريع  
✅ لا حاجة لـ manual submission  
✅ يعمل كـ native mobile app  
✅ PWA compliant  
✅ اختبار بسيط من DevTools

---

**كل شيء جاهز الآن!** 🎉
